#!/bin/sh

# takes one argument: the label for the next step number
stepnum=$1

# first, we define a list of the replicas that we need to swap with each
# other segment

segment$SEGA = "$REPSA"
segment$SEGB = "$REPSB"


# now we need to make the next directories
# these assume that the PWD is the segment$THISSEG/interset$PREVSTEPNUM

# if our swap partners' directories don't exist, make them
for seg in $SEGA $SEGB
do
    if [ -ne 
done
